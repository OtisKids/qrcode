qr_config:
  version: null  # Auto-detect
  error_correction: H  # L, M, Q, H
  box_size: 10
  border: 4

style_config:
  style_intensity: 0.3  # 0.0 to 1.0
  contrast_threshold: 0.4
  preserve_finder_patterns: true
  module_style: square  # square, rounded, circular, gapped
  use_gradient: false